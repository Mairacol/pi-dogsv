{
    "version": "1.0",
    "build": {
      "commands": [
        "npm ci --omit=dev",
        "npm run build --prefix client", 
        "npm install --omit=dev && npm run build" 
      ]
    },
    "start": {
      "command": "node index.js" 
    }
  }
  